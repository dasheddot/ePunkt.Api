@model ePunkt.Portal.Models.Application.RefreshViewModel

<h1>@Html.Loc("Title")</h1>
<p>@Html.Loc("Description")</p>

<p>
    @Html.ActionLink(Html.Loc("GoToProfile").ToString(), "Index", "Applicant")
</p>

<p>
    @Html.ActionLink(Html.Loc("Continue").ToString(), "Index", "Application", new { ignoreDuplicate = true, job = Model.JobId }, null)
</p>
